version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: game-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Havilah19$
      MYSQL_DATABASE: main_db
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: