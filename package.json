{
  "name": "altschool-game",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix guessing-game-backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}